name: Daily Telegram Bot

on:
  schedule:
    - cron: '0 4 * * *'  # Запуск о 04:00 UTC (07:00 за Києвом)
  workflow_dispatch:     # Цей рядок залишає можливість запускати бота вручну кнопкою

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run script
      env:
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}  # Передаємо ключ для ШІ
      run: python bot.py
